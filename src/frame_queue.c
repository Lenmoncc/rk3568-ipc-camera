/*
 * frame_queue.c — 原始数据有界队列
 *
 * 当前阶段：只建立模块文件和对外接口，尚未实现任何业务函数。
 * 不使用“直接返回成功”的空实现；接入 main 前必须补齐本模块定义。
 *
 * 后续实现任务：
 * 1. 实现互斥锁、条件变量、有界存储及非阻塞 try_push。
 * 2. 关闭后排空已有元素；唤醒等待线程并明确空队列退出语义。
 * 3. 管理 IpcRawFrame 生命周期；数据销毁不得与在途消费者并发。
 */
#include "frame_queue.h"

/* TODO：按头文件契约逐步实现。 */
