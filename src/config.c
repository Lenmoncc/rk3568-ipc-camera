/*
 * config.c — 读取与校验配置
 *
 * 当前阶段：只建立模块文件和对外接口，尚未实现任何业务函数。
 * 不使用“直接返回成功”的空实现；接入 main 前必须补齐本模块定义。
 *
 * 后续实现任务：
 * 1. 解析 configs/ipc.conf，拒绝未知键、溢出值和必要参数缺失。
 * 2. 校验 720P NV12 / H.264 基线及实际音频参数。
 * 3. 输出可用于排查的配置摘要；不在此模块打开任何硬件设备。
 */
#include "config.h"

/* TODO：按头文件契约逐步实现。 */
