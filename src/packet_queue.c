/*
 * packet_queue.c — 编码包有界队列
 *
 * 当前阶段：只建立模块文件和对外接口，尚未实现任何业务函数。
 * 不使用“直接返回成功”的空实现；接入 main 前必须补齐本模块定义。
 *
 * 后续实现任务：
 * 1. 使用 FFmpeg 4.4.1 的 AVPacket 引用接口管理压缩数据。
 * 2. 两路输出分别持有 packet 对象，不共享可变时间戳或流索引字段。
 * 3. 输出线程需协调音视频两队列的等待，不可永久堵在单个空队列。
 */
#include "packet_queue.h"

/* TODO：按头文件契约逐步实现。 */
