/*
 * mp4_output.c — 本地 MP4 录像
 *
 * 当前阶段：只建立模块文件和对外接口，尚未实现任何业务函数。
 * 不使用“直接返回成功”的空实现；接入 main 前必须补齐本模块定义。
 *
 * 后续实现任务：
 * 1. 建立独立于网络的封装上下文，复制编码参数。
 * 2. 协调音视频队列、重映射流索引和时间基，交错写入。
 * 3. 正常结束时排空队列并写 trailer；失败文件不得标记为完整录像。
 * 4. 初版不实现分段录像和断电恢复。
 */
#include "mp4_output.h"

/* TODO：按头文件契约逐步实现。 */
