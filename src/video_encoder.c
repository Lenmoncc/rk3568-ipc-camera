/*
 * video_encoder.c — MPP H.264 硬编码
 *
 * 当前阶段：只建立模块文件和对外接口，尚未实现任何业务函数。
 * 不使用“直接返回成功”的空实现；接入 main 前必须补齐本模块定义。
 *
 * 后续实现任务：
 * 1. 按 SDK 实际 MPP 接口建立编码器，处理 NV12 步长和输入内存生命周期。
 * 2. 获取 SPS/PPS 等流参数并通知输出模块就绪。
 * 3. 持续提交帧、收取包；为 RTMP 和 MP4 分别分发包引用。
 * 4. 输入结束后排空编码器，再关闭对应的两路视频包队列。
 */
#include "video_encoder.h"

/* TODO：按头文件契约逐步实现。 */
