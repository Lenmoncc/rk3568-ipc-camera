/*
 * rtmp_output.c — FLV 封装与 RTMP 输出
 *
 * 当前阶段：只建立模块文件和对外接口，尚未实现任何业务函数。
 * 不使用“直接返回成功”的空实现；接入 main 前必须补齐本模块定义。
 *
 * 后续实现任务：
 * 1. 等 H.264/AAC 流参数就绪后复制 codecpar，并写入 FLV 头。
 * 2. 协调音视频队列，转换到实际流时间基并交错写包。
 * 3. 设置网络超时与可中断退出；初版不自动重连。
 * 4. 异常时停止该路分发并释放队列，不能拖住本地录像。
 */
#include "rtmp_output.h"

/* TODO：按头文件契约逐步实现。 */
