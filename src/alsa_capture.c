/*
 * alsa_capture.c — ALSA 音频采集
 *
 * 当前阶段：只建立模块文件和对外接口，尚未实现任何业务函数。
 * 不使用“直接返回成功”的空实现；接入 main 前必须补齐本模块定义。
 *
 * 后续实现任务：
 * 1. 打开配置中的 hw:0,0，使用用户已验证的实际音频参数。
 * 2. 处理短读、XRUN 和退出；独立保存每个 PCM 块。
 * 3. 记录累计采样位置和起点；停止后关闭音频原始队列。
 */
#include "alsa_capture.h"

/* TODO：按头文件契约逐步实现。 */
